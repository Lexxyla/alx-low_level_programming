#include "main.h"

/**
 * prints the numbers, from 0 to 9, followed by a new line.
 * Return: none
 */
void print_most_numbers(void)
{
	int ch;
	for (ch = 48; ch < 58; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
